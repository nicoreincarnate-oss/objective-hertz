# Perseus core dependencies
click>=8.0.0
ddgs>=9.11.4
python-dotenv>=1.0.0
httpx>=0.27.0
openai>=1.30.0
requests>=2.31.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.2.0
rich>=13.0.0

# Hermes (Telegram bot)
python-telegram-bot>=21.0

# Web dashboard
fastapi>=0.115.0
uvicorn>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.9
prometheus-client>=0.24.1
sentry-sdk>=2.55.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
ruff>=0.11.0
mypy>=1.11.0

# Note: these are NOT needed if using Claude Max (which provides API access)
# Install only if using direct API calls:
# anthropic>=0.40.0

types-requests>=2.32.0
respx>=0.22

# Full Integration (Phase 1+)
apify-client>=2.5.0
tavily-python>=0.7.0
claude-agent-sdk>=0.1.50
mcp>=1.0.0
# mlx-lm>=0.20.0  # macOS only — Apple Silicon LoRA fine-tuning
