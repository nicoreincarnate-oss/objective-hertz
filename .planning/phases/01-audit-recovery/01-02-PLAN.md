---
phase: 01-audit-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/escalation_log/redactor.py
  - scripts/rotate_escalation_log.py
  - scripts/launchagents/com.perseus.hermes.plist
  - scripts/launchagents/com.perseus.conway.plist
  - scripts/launchagents/com.perseus.deerflow.plist
  - scripts/launchagents/com.perseus.ruflo.plist
  - scripts/launchagents/com.perseus.openjarvis.plist
  - scripts/launchagents/com.perseus.clawdbot-health.plist
  - shared/lead_worker.py
  - docs/compliance/local-tier-licenses.md
  - docs/compliance/ruflo-cutover-soak.md
  - docs/spikes/gbnf-mlx-vlm-spike.md
  - docs/spikes/airllm-heavy-spike.md
  - docs/spikes/litellm-pre-call-hook-spike.md
  - litellm/eval/golden/.gitkeep
  - litellm/hooks/local_path_guard.py
  - litellm/routing_policy.yaml
  - hermes/web/app.py
autonomous: true
requirements:
  - P1-1
  - P1-2
  - P1-7
  - P1-9
  - P1-13
  - P1-15
user_setup: []

must_haves:
  truths:
    - "redactor.py redacts OpenRouter keys, Telegram bot tokens, Slack webhooks, ETH private keys, and Postgres connection strings"
    - "scripts/rotate_escalation_log.py rotates the encrypted log at 100MB or 7 days, keeps 10 rotations, preserves the salt file"
    - "6 daemon launchd plists exist under scripts/launchagents/ with EnvironmentVariables, healthcheck wiring, and ExitTimeOut"
    - "shared/lead_worker.py deep-copies context dict before passing to sub-agents (no mutation of caller's dict)"
    - "7 spec'd-but-missing artifact stubs exist (compliance docs, spike results, litellm hook, routing policy)"
  artifacts:
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/escalation_log/redactor.py
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/rotate_escalation_log.py
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.hermes.plist
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.conway.plist
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.deerflow.plist
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.ruflo.plist
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.openjarvis.plist
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.clawdbot-health.plist
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/lead_worker.py
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/compliance/local-tier-licenses.md
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/compliance/ruflo-cutover-soak.md
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/spikes/gbnf-mlx-vlm-spike.md
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/spikes/airllm-heavy-spike.md
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/spikes/litellm-pre-call-hook-spike.md
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/litellm/hooks/local_path_guard.py
    - /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/litellm/routing_policy.yaml
  key_links:
    - "Each new redactor pattern has a matching unit test case or regex comment explaining the match class"
    - "Each daemon plist's ProgramArguments matches a real `python -m {daemon}.daemon` startup"
---

<objective>
Land all Wave R3 operational P1 fixes in parallel: missing redactor patterns, escalation log rotation script, 6 missing daemon launchd plists, LeadWorkerLoop context dict mutation fix, stub files for spec'd-but-missing artifacts, and the hermes DASHBOARD_HOST localhost fix (if applicable).

Purpose: Clear as many P1s as possible overnight so the operator wakes up with a materially reduced audit backlog. None of these tasks require operator decisions — all are well-specified fixes.

Output: 1 modified redactor, 1 new rotation script, 6 new plists, 1 modified lead_worker, 7 stub files, and up to 1 modified hermes app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-audit-recovery/01-CONTEXT.md
@docs/audits/pre-launch/SUMMARY.md
@docs/audits/pre-launch/11-tech-debt.md
@scripts/launchagents/com.perseus.titan.plist
@scripts/launchagents/com.perseus.clawdbot.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 missing redactor patterns (P1-2)</name>
  <files>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/escalation_log/redactor.py</files>
  <read_first>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/escalation_log/redactor.py</read_first>
  <action>
Add 5 new redaction patterns to the existing pattern list/registry in redactor.py. Use the EXACT regex strings below (they're calibrated for real-world keys):

  1. OpenRouter API keys: `sk-or-v1-[A-Za-z0-9\-_]{48,}`
  2. Telegram bot tokens: `\d{9,10}:[A-Za-z0-9_\-]{35}`
  3. Slack webhooks: `https://hooks\.slack\.com/services/[A-Z0-9]+/[A-Z0-9]+/[A-Za-z0-9]+`
  4. ETH private keys (64-hex): `0x[a-fA-F0-9]{64}`
  5. Postgres connection strings: `postgres(?:ql)?://[^@]+@[^/]+/\w+`

Each pattern should be added to the same mechanism the existing patterns use (don't invent a new registry). Each should have a replacement token like `<REDACTED:OPENROUTER_KEY>`, `<REDACTED:TELEGRAM_TOKEN>`, `<REDACTED:SLACK_WEBHOOK>`, `<REDACTED:ETH_PRIVKEY>`, `<REDACTED:POSTGRES_URL>`. Add a short comment above each pattern documenting what it matches.

Do NOT touch the scrypt KDF or fail-closed code (P0-5/P0-6 already landed).
  </action>
  <verify>
python3 -c "
import re
from shared.escalation_log.redactor import redact  # or equivalent public entry
samples = [
    ('sk-or-v1-' + 'a'*50, 'OPENROUTER'),
    ('1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghi', 'TELEGRAM'),
    ('https://hooks.slack.com/services/T00/B00/XXXXabcd', 'SLACK'),
    ('0x' + 'a'*64, 'ETH'),
    ('postgresql://user:pw@host/db', 'POSTGRES'),
]
for s, label in samples:
    out = redact(s)
    assert s not in out, f'{label} NOT redacted'
    print(label, 'OK')
"
  </verify>
  <acceptance_criteria>
    - `grep -c "sk-or-v1" /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/escalation_log/redactor.py` >= 1
    - All 5 regex literals present in file
    - All 5 REDACTED replacement tokens present in file
    - Existing tests still pass
  </acceptance_criteria>
  <done>5 patterns added, redact() proven to mask all 5 classes. Atomic commit: "feat(redactor): P1-2 add 5 missing secret patterns".</done>
</task>

<task type="auto">
  <name>Task 2: Write escalation log rotation script (P1-9)</name>
  <files>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/rotate_escalation_log.py</files>
  <read_first>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/escalation_log/redactor.py</read_first>
  <action>
Create a new Python 3 script at scripts/rotate_escalation_log.py that rotates the encrypted escalation log based on:

  - Size trigger: 100 * 1024 * 1024 bytes (100 MB)
  - Age trigger: 7 days (use mtime)
  - Retention: keep 10 rotations, delete older
  - Naming: escalation.log.1, escalation.log.2, ... (standard logrotate style)
  - Preserve the salt file (DO NOT touch any `*.salt` file in the log directory)

Script entry:
  - `if __name__ == "__main__": main()`
  - `main()` reads log path from env var `PERSEUS_ESCALATION_LOG_PATH` (default: `~/Library/Logs/perseus/escalation.log`)
  - Uses `argparse` for `--dry-run` flag
  - Logs rotation actions to stderr
  - Exit code 0 on success, 1 on error

Use only stdlib (os, shutil, pathlib, argparse, time, sys). The script is intended to be run from launchd or cron.
  </action>
  <verify>python3 /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/rotate_escalation_log.py --dry-run; echo "exit: $?"</verify>
  <acceptance_criteria>
    - File exists, is executable-as-python (no syntax errors)
    - `--dry-run` flag exits 0 without touching files
    - Script references `100 * 1024 * 1024` or `104857600`
    - Script references `7` days or `604800` seconds
    - Script has explicit logic preserving `.salt` files
  </acceptance_criteria>
  <done>Rotation script landed + dry-run tested. Atomic commit: "feat(ops): P1-9 escalation log rotation script (100MB / 7d / keep 10)".</done>
</task>

<task type="auto">
  <name>Task 3: Write 6 missing daemon launchd plists (P1-13)</name>
  <files>
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.hermes.plist,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.conway.plist,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.deerflow.plist,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.ruflo.plist,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.openjarvis.plist,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.clawdbot-health.plist
  </files>
  <read_first>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.titan.plist, /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.clawdbot.plist</read_first>
  <action>
For each of the 6 daemons below, write a plist matching the style of existing titan/clawdbot plists. Before writing each, check whether the daemon module actually exists at `/Users/majovega/Desktop/Projects/objective-hertz/{daemon}/` in the main repo — if not (e.g., ruflo.plist may already exist), skip that file and note it in the summary.

Daemons and startup commands:
  - hermes → `python -m hermes.daemon` (ports 9002 Telegram, 8500 war-room)
  - conway → `python -m conway.daemon`
  - deerflow → `python -m deerflow_research.daemon`
  - ruflo → `python -m ruflo.daemon` (port 9004) — SKIP if com.perseus.ruflo.plist already exists
  - openjarvis → `python -m openjarvis.daemon`
  - clawdbot-health → healthcheck-only plist running `python -m clawdbot.healthcheck` every 60s via `StartInterval`

Required keys in each plist (reference titan.plist for exact XML formatting):
  - Label: com.perseus.{daemon}
  - ProgramArguments: python3 -m {module} (use /opt/homebrew/bin/python3 or /usr/local/bin/python3 — match titan's choice)
  - WorkingDirectory: /Users/majovega/Desktop/Projects/objective-hertz
  - EnvironmentVariables: must include `PATH` and load from .env — match titan.plist's approach (if titan uses a wrapper script, use the same wrapper)
  - RunAtLoad: true
  - KeepAlive: true (except clawdbot-health which uses StartInterval=60, KeepAlive false)
  - ThrottleInterval: 10
  - ExitTimeOut: 30 (P1-10 requirement)
  - StandardOutPath: /Users/majovega/Library/Logs/perseus/{daemon}.log
  - StandardErrorPath: /Users/majovega/Library/Logs/perseus/{daemon}.error.log
  </action>
  <verify>
for f in hermes conway deerflow ruflo openjarvis clawdbot-health; do
  p=/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/scripts/launchagents/com.perseus.$f.plist
  if [ -f "$p" ]; then plutil -lint "$p"; fi
done
  </verify>
  <acceptance_criteria>
    - At least 5 of 6 plists exist (ruflo may be pre-existing)
    - Every new plist passes `plutil -lint`
    - Every new plist contains `<key>ExitTimeOut</key>` followed by `<integer>30</integer>`
    - Every new plist references `/Users/majovega/Library/Logs/perseus/`
  </acceptance_criteria>
  <done>5-6 daemon plists committed. Atomic commit: "chore(launchd): P1-13 add missing daemon plists with healthchecks and ExitTimeOut".</done>
</task>

<task type="auto">
  <name>Task 4: Fix LeadWorkerLoop context dict mutation (P1-7)</name>
  <files>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/lead_worker.py</files>
  <read_first>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/lead_worker.py</read_first>
  <action>
Find the location in lead_worker.py where the lead passes a `context` (or similarly named) dict to sub-agents / workers. Before passing, replace any shared reference with `copy.deepcopy(context)`. Add `import copy` at top if not already present.

The fix must:
  - Use `copy.deepcopy`, NOT dict.copy() (the nested mutation is the bug — shallow copy won't fix it)
  - Only deep-copy on the outbound path to workers; the lead can still mutate its own local copy
  - Add a 1-line comment explaining why (`# deepcopy: workers must not see each other's mutations — P1-7`)

If the file has no obvious context-dict pass-through, read it carefully to find the sub-agent invocation (likely a `gather`, `run_worker`, `spawn`, or `dispatch` method) and fix it there.
  </action>
  <verify>grep -n "deepcopy" /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/lead_worker.py</verify>
  <acceptance_criteria>
    - `import copy` present in lead_worker.py
    - At least one `copy.deepcopy(` call present
    - Comment containing "P1-7" present on or near the deepcopy line
  </acceptance_criteria>
  <done>lead_worker.py deep-copies context. Atomic commit: "fix(lead-worker): P1-7 deepcopy context before dispatching to workers".</done>
</task>

<task type="auto">
  <name>Task 5: Create stub files for spec'd-but-missing artifacts (P1-1)</name>
  <files>
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/compliance/local-tier-licenses.md,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/compliance/ruflo-cutover-soak.md,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/spikes/gbnf-mlx-vlm-spike.md,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/spikes/airllm-heavy-spike.md,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/docs/spikes/litellm-pre-call-hook-spike.md,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/litellm/eval/golden/.gitkeep,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/litellm/hooks/local_path_guard.py,
    /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/litellm/routing_policy.yaml
  </files>
  <read_first></read_first>
  <action>
Create stub files (not implementations — these are placeholders that satisfy the audit's missing-artifact findings):

  1. `docs/compliance/local-tier-licenses.md` — heading + "TODO: document license status for Qwen3-30B-A3B, Qwen2.5-Coder-14B, Qwen3-8B, Qwen2.5-VL, Parakeet, Kokoro. Per operator rule: no commercial license concerns apply. This doc exists for completeness only."
  2. `docs/compliance/ruflo-cutover-soak.md` — heading + "TODO: 72h soak protocol for Ruflo local tier cutover. Metrics: fix success rate, avg latency, sandbox violations, cost delta. Blocked on Phase 42.5 v2 merge."
  3. `docs/spikes/gbnf-mlx-vlm-spike.md` — heading + "TODO: spike result for GBNF grammar compilation + MLX VLM integration. See shared/verifier/grammar_compiler.py for current compile path."
  4. `docs/spikes/airllm-heavy-spike.md` — heading + "TODO: AirLLM Llama 70B heavy tier spike. Blocked on Samsung T9 NVMe arrival (1-2 weeks)."
  5. `docs/spikes/litellm-pre-call-hook-spike.md` — heading + "TODO: LiteLLM pre-call hook spike. Referenced by litellm/hooks/local_path_guard.py stub."
  6. `litellm/eval/golden/.gitkeep` — empty file (just ensure directory exists in git)
  7. `litellm/hooks/local_path_guard.py` — stub with a module docstring explaining its purpose, a `NotImplementedError`-raising function stub `def enforce_local_path_policy(request): raise NotImplementedError("P1-1 stub — see docs/spikes/litellm-pre-call-hook-spike.md")`
  8. `litellm/routing_policy.yaml` — YAML stub with a comment header and one commented-out example routing rule

All stubs must clearly state they are P1-1 placeholders and point at the follow-up work.
  </action>
  <verify>
for f in docs/compliance/local-tier-licenses.md docs/compliance/ruflo-cutover-soak.md docs/spikes/gbnf-mlx-vlm-spike.md docs/spikes/airllm-heavy-spike.md docs/spikes/litellm-pre-call-hook-spike.md litellm/eval/golden/.gitkeep litellm/hooks/local_path_guard.py litellm/routing_policy.yaml; do
  test -f /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/$f && echo OK $f || echo MISSING $f
done
python3 -c "import ast; ast.parse(open('/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/litellm/hooks/local_path_guard.py').read())"
python3 -c "import yaml; yaml.safe_load(open('/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/litellm/routing_policy.yaml'))"
  </verify>
  <acceptance_criteria>
    - All 8 files exist
    - local_path_guard.py is valid Python
    - routing_policy.yaml is valid YAML
    - Each stub contains the string "P1-1"
  </acceptance_criteria>
  <done>8 stub files landed. Atomic commit: "chore(stubs): P1-1 placeholder artifacts for spec'd-but-missing files".</done>
</task>

<task type="auto">
  <name>Task 6: DASHBOARD_HOST localhost check (P1-15)</name>
  <files>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/hermes/web/app.py</files>
  <read_first>/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/hermes/web/app.py</read_first>
  <action>
CONDITIONAL: if `/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/hermes/web/app.py` does NOT exist in the worktree, this task is a no-op — record "hermes/web/app.py not in worktree, P1-15 deferred to main" in the task summary and skip.

If the file exists: find the `DASHBOARD_HOST` default and change from `0.0.0.0` to `127.0.0.1`. The change should be the default value in either an env-var lookup (`os.getenv("DASHBOARD_HOST", "127.0.0.1")`) or a config constant. DO NOT change anything that reads from a user-provided config file — only the hard-coded default.
  </action>
  <verify>
if [ -f /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/hermes/web/app.py ]; then
  grep -n "DASHBOARD_HOST" /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/hermes/web/app.py
  ! grep -n '0\.0\.0\.0' /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/hermes/web/app.py
else
  echo "FILE NOT IN WORKTREE — task is no-op"
fi
  </verify>
  <acceptance_criteria>
    - If file exists: default is `127.0.0.1`, no `0.0.0.0` in file
    - If file does not exist: task summary records the deferral
  </acceptance_criteria>
  <done>Either fixed with atomic commit "fix(hermes): P1-15 bind dashboard to localhost by default" or deferred with note.</done>
</task>

</tasks>

<verification>
Plan-level success = Tasks 1-5 fully done + Task 6 resolved (done or deferred-with-note). Must_haves.truths 1-5 all true.

Independence: Tasks 1-6 are fully independent (different files, no shared state). Recommended execution:

  Wave A (fully parallel, 6 subagents): Task 1, Task 2, Task 3, Task 4, Task 5, Task 6

Atomic commit per task so operator can revert individual fixes if needed.

Testability: Every task has a concrete grep or python import/parse check. Verifier runs each `verify` block.
</verification>

<dependencies>
Blocks: 01-03-PLAN.md (Wave R4 verification — Task 1 here must have landed before verification runs the redactor import spot check).
Blocked by: nothing — can run fully in parallel with 01-01-PLAN.md.
</dependencies>
