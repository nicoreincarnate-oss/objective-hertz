---
phase: 02
plan: 02
type: execute
wave: 2
depends_on: [02-01]
title: Tier Core Merge
effort_hours: 9
---

# Phase 2 Plan 02 — Tier Core Merge

**Wave 2 of Phase 2.** Merge worktree's `TierName` enum into main's `ModelTier`, refactor `tier_classifier.py` and `lead_worker.py` to use the merged type, preserve P1-7 (LeadWorkerLoop deepcopy fix already landed in Phase 1).

## Depends on

Plan 02-01 must complete first. Specifically we need:
- Rebase onto main done (shared/llm_client.py is 1711 lines)
- `shared/llm_unified.ModelTier` enum accessible

## Tasks

### Task 1: REFACTOR `shared/tiers.py` → merge into main `ModelTier`
**Effort:** 4h
**Files:**
- `/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/llm_unified.py` (main's, extend)
- `/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/tiers.py` (worktree's, adapt)

- Read `shared/llm_unified.py` to see the existing `ModelTier` enum and its 7 current values
- Extend `ModelTier` with 6 new values: `CODEX`, `AGENTIC`, `LONGCTX`, `CHAT`, `CHEAP`, `VISION`
- Preserve worktree's `TierConfig` dataclass in `shared/tiers.py` as metadata store (main has no equivalent)
- In `shared/tiers.py`: rewrite `class TierName(str, Enum)` → `TierName = ModelTier` (alias assignment). This keeps all downstream code working via `from shared.tiers import TierName` which now resolves to ModelTier.
- Add the `auto → smart` alias to main's `_MODEL_MAP` in `shared/llm_client.py` if not already present (per decision #1 — worktree wins)
- Preserve worktree's `TIERS: dict[TierName, TierConfig]` metadata registry
- Verify: `python -c "from shared.tiers import TierName; from shared.llm_unified import ModelTier; assert TierName is ModelTier; print('tier merge OK')"`
- Verify: `python -c "from shared.tiers import TIERS, TierName; assert TierName.CODEX in TIERS and TierName.VISION in TIERS; print('TierConfig registry OK')"`
- Commit: `refactor(tiers): merge TierName into ModelTier, extend with 6 new tiers`

### Task 2: REFACTOR `shared/tier_classifier.py` → rewire imports
**Effort:** 3h
**Files:** `/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/tier_classifier.py`

- Since `shared/tiers.py` now re-exports `TierName = ModelTier`, this file's imports should Just Work
- Verify all rule keywords still map to merged tier names (vision/chat rules should hit `TierName.VISION` and `TierName.CHAT` which now exist)
- If any rules refer to tiers that DIDN'T get added in Task 1 above, error out with a clear message
- Add opt-in hook in main's `shared/llm_client.py:LLMClient.generate`: if caller passes `auto_tier=True`, call `RuleBasedClassifier().classify(prompt, daemon_name=daemon_name)` before `_resolve_model`. Default `auto_tier=False` to avoid changing existing behavior.
- Verify: `python -c "from shared.tier_classifier import RuleBasedClassifier; c = RuleBasedClassifier(); r = c.classify('write a fast email'); print(r.tier, r.confidence)"` — should return a valid ModelTier
- Commit: `refactor(tier-classifier): rewire to ModelTier + add auto_tier hook in llm_client.generate`

### Task 3: REFACTOR `shared/lead_worker.py` → verify still works post-merge
**Effort:** 2h
**Files:** `/Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/shared/lead_worker.py`

- Verify import still works: `from shared.tiers import TierName` (now aliased to ModelTier)
- Verify P1-7 preservation: all 5 `copy.deepcopy(context)` sites still present (should be if rebase kept Phase 1 commits)
- Spot check: `python -c "from shared.lead_worker import LeadWorkerLoop, LeadWorkerConfig, Step, StepResult; print('imports OK')"`
- Verify the generic loop still accepts ModelTier values: `python -c "from shared.lead_worker import LeadWorkerConfig; from shared.tiers import TierName; c = LeadWorkerConfig(lead_tier=TierName.SMART, worker_tier=TierName.FAST); print(c)"`
- If lead_worker internally resolved tiers via worktree's `TIERS` dict: verify that dict still has entries for all referenced tiers
- No code changes expected — this is mostly a verification task. If anything breaks, fix inline.
- Commit: `refactor(lead-worker): verify post-merge compatibility, preserve P1-7 deepcopy`

## Verification

After 02-02 tasks complete, run:

1. `python -c "from shared.tiers import TierName; from shared.llm_unified import ModelTier; assert TierName is ModelTier"` — merge verified
2. `python -c "from shared.tiers import TIERS; assert len(TIERS) >= 11"` — all tiers have TierConfig metadata
3. `python -c "from shared.tier_classifier import RuleBasedClassifier; c = RuleBasedClassifier(); assert c.classify('test').tier"` — classifier returns valid tier
4. `python -c "from shared.lead_worker import LeadWorkerLoop; import inspect; src = inspect.getsource(LeadWorkerLoop); assert 'copy.deepcopy' in src"` — P1-7 preserved
5. `pytest /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/tests/test_phase41_tiers.py /Users/majovega/Desktop/Projects/objective-hertz/.claude/worktrees/charming-elion/tests/test_phase43_lead_worker.py -v` — must pass

Write SUMMARY.md to `02-02-SUMMARY.md` with task status and commit SHAs.

## Rollback

If tier merge produces unresolvable type errors in `tier_classifier.py` or `lead_worker.py`:
- `git revert HEAD~N..HEAD` for the Task 1-3 commits
- Record in HANDOFF.md PHASE-2-BLOCKER section
- Return PARTIAL verdict with what did land
