---
plan: 05-02
phase: 5
name: Shadow Mode + Feature Flags + Tests
wave: 2
autonomous: true
req_ids: [RLM-06, RLM-07]
objective: >
  Wire RLMComposer into email_compose.py with shadow mode A/B testing,
  feature flag with instant rollback (system_config), and comprehensive test suite.
files_modified:
  - titan/pipeline/email_compose.py
  - tests/titan/test_rlm_composer.py
task_count: 3
---

# Plan 05-02: Shadow Mode + Feature Flags + Tests

## Objective

Wire RLM into email pipeline with A/B testing and comprehensive tests.

Read `.planning/phases/5/PLAN.md` Tasks 6-8 for details.

## Tasks

### Task 1: Shadow mode A/B testing (RLM-06)

**File:** `titan/pipeline/email_compose.py`

Add RLM integration to compose_email:
- `RLM_ENABLED=true` + `RLM_SHADOW_MODE=true` → run both paths, log comparison, return original
- `RLM_ENABLED=true` + `RLM_SHADOW_MODE=false` → full RLM compose
- `RLM_ENABLED=false` → original single-pass compose
- `_log_ab_comparison()` stores original vs RLM scores for analysis
- Shadow period configurable

**Commit:** `feat(05-02): wire RLM into email pipeline with shadow mode A/B testing`

### Task 2: Feature flag with runtime toggle (RLM-07)

**File:** `titan/pipeline/email_compose.py` (extend)

- Check system_config table for runtime toggle (not just env var)
- Instant rollback: set RLM_ENABLED=false in system_config
- State stored in system_config for operator control via dashboard

**Commit:** `feat(05-02): add runtime feature flag toggle via system_config`

### Task 3: Test suite

**File:** `tests/titan/test_rlm_composer.py` (new)

Tests:
- Recursive compose returns highest-scoring version
- Budget cap stops iteration and returns best draft
- Shadow mode logs comparison but returns original
- Qdrant retrieval returns relevant context (mock)
- Mem0 stores research context (mock)
- Feature flag off = original compose
- Feature flag on, shadow = both paths run
- Feature flag on, no shadow = RLM only
- Context expansion triggers on low specificity
- Monthly spend cap fallback to single-pass

Run: `PYTHONPATH=. pytest tests/titan/test_rlm_composer.py -v`

**Commit:** `test(05-02): add RLM test suite — compose, budget, shadow, flags`

## Acceptance Criteria

- [ ] Shadow mode captures comparison data
- [ ] Feature flag toggles cleanly
- [ ] All tests pass
- [ ] `ruff check` clean on modified files
