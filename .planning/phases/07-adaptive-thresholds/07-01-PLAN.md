---
plan: 07-01
phase: 7
name: Bandit + DB + Experiments + Migration
wave: 1
autonomous: true
req_ids: [ADAPT-01, ADAPT-02, ADAPT-03, ADAPT-04, ADAPT-05, ADAPT-06]
objective: >
  Build Thompson sampling bandit (clean-room Sutton & Barto), DB-stored thresholds
  with warm-start priors, pipeline outcome training signal, concurrent A/B experiments,
  meta_evaluations audit table. Migration 023 creates adaptive_thresholds + meta_evaluations.
files_modified:
  - titan/adaptive_thresholds.py
  - scripts/migrations/023-adaptive-thresholds.sql
task_count: 6
---

# Plan 07-01: Bandit + DB + Experiments + Migration

Read `.planning/phases/7/PLAN.md` Tasks 1-6 for full details.

## Tasks

### Task 1: BetaBandit + AdaptiveThresholds (ADAPT-01)
**File:** `titan/adaptive_thresholds.py` (new)
Thompson sampling from Beta distribution. Implements ThresholdProvider Protocol.
**Commit:** `feat(07-01): add Thompson sampling BetaBandit + AdaptiveThresholds`

### Task 2: Migration 023 + DB persistence (ADAPT-02)
**Files:** `scripts/migrations/023-adaptive-thresholds.sql`, `titan/adaptive_thresholds.py` (extend)
Create adaptive_thresholds table + meta_evaluations table. Seed 5 thresholds with Beta(10,2).
**Commit:** `feat(07-01): add migration 023 + DB-stored bandit state`

### Task 3: Pipeline outcome training (ADAPT-03)
**File:** `titan/adaptive_thresholds.py` (extend)
`collect_training_signal()` from leads table conversions. Daily job.
**Commit:** `feat(07-01): add pipeline outcome training signal collection`

### Task 4: Warm-start priors (ADAPT-04)
Already handled by migration seed — Beta(10,2) for all 5 thresholds.
**Commit:** (combined with Task 2)

### Task 5: ExperimentManager (ADAPT-05)
**File:** `titan/adaptive_thresholds.py` (extend)
Concurrent A/B shadow experiments with 50/50 assignment and statistical eval.
**Commit:** `feat(07-01): add ExperimentManager for concurrent A/B threshold tests`

### Task 6: meta_evaluations table (ADAPT-06)
Already in migration 023. Add logging to AdaptiveThresholds.update().
**Commit:** (combined with Task 2)

## Acceptance Criteria
- [ ] Thompson sampling from Beta distribution
- [ ] DB persistence across restarts
- [ ] Training signal from pipeline outcomes
- [ ] 2+ concurrent experiments
- [ ] meta_evaluations logs all changes
- [ ] `isinstance(AdaptiveThresholds(), ThresholdProvider)` passes
- [ ] Zero HyperAgents code (clean-room only)
