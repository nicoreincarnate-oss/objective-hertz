---
plan: 04-02
phase: 4
name: Engine Integration + Tests
wave: 2
autonomous: true
req_ids: [MW-01]
objective: >
  Wire middleware chain into WorkflowEngine._execute_node_async() behind
  ENABLE_MIDDLEWARE feature flag. Write comprehensive test suite for chain
  execution, all middlewares, ordering, and pipeline integration.
files_modified:
  - openjarvis/workflow/engine.py
  - tests/shared/test_middleware.py
task_count: 2
---

# Plan 04-02: Engine Integration + Tests

## Objective

Wire middleware into the async engine and write comprehensive tests.

Read `.planning/phases/4/PLAN.md` Tasks 8-9 for details.

## Tasks

### Task 1: WorkflowEngine middleware integration

**File:** `openjarvis/workflow/engine.py`

In `_execute_node_async()`:
- Check ENABLE_MIDDLEWARE feature flag
- Build chain from pipeline config
- Wrap node execution: `chain.execute(stage_ctx, node_handler)`
- Stage ctx includes: daemon_name, stage_name, pipeline, tools
- Flag off = no middleware, zero change

**Commit:** `feat(04-02): wire middleware chain into WorkflowEngine node execution`

### Task 2: Full test suite

**File:** `tests/shared/test_middleware.py` (new)

Tests:
- Chain execution order (first added = outermost)
- MemoryMiddleware: injects memories pre-stage, saves post-stage
- DNAGuard: blocks unauthorized tools, passes authorized
- AntiSlop: scores content stages, passes non-content
- TelemetryMiddleware: records timing to stage_metrics (mock DB)
- BudgetCheck: blocks over-budget, passes under-budget
- Configurable ordering: PIPELINE_CONFIGS dict
- Env override: TITAN_MIDDLEWARE_ORDER
- Full chain integration: all 5 middlewares in sequence
- Feature flag off = no middleware

Run: `PYTHONPATH=. pytest tests/shared/test_middleware.py -v`

**Commit:** `test(04-02): add middleware test suite — chain, guards, telemetry, integration`

## Acceptance Criteria

- [ ] Middleware chain executes on every engine node when flag enabled
- [ ] Feature flag off = zero change
- [ ] All middleware tests pass
- [ ] No regression on existing async engine tests
- [ ] `ruff check` clean
