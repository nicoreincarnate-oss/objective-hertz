---
phase: 10-21st-dev-recraft-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/twentyfirst_client.py
  - clawdbot/design_sources.py
  - tests/test_twentyfirst_client.py
  - tests/test_design_sources.py
autonomous: true
requirements:
  - TWENTY1-01
  - TWENTY1-02
  - TWENTY1-03

must_haves:
  truths:
    - "21st.dev REST API can be called from Python and returns component snippets"
    - "design_sources.py enriches curated sources with real component code when API key is available"
    - "Component code is framed as structural inspiration, not copy-paste React"
    - "Missing API key degrades gracefully to current text-only behavior"
  artifacts:
    - path: "tools/twentyfirst_client.py"
      provides: "21st.dev REST API client"
      exports: ["fetch_component_inspiration"]
      min_lines: 40
    - path: "clawdbot/design_sources.py"
      provides: "Enriched design source resolution with real component code"
      exports: ["resolve_design_sources", "resolve_design_sources_with_components"]
      min_lines: 80
    - path: "tests/test_twentyfirst_client.py"
      provides: "Client unit tests"
      min_lines: 50
    - path: "tests/test_design_sources.py"
      provides: "Extended design source tests including component enrichment"
      min_lines: 50
  key_links:
    - from: "clawdbot/design_sources.py"
      to: "tools/twentyfirst_client.py"
      via: "import fetch_component_inspiration"
      pattern: "from tools\\.twentyfirst_client import"
    - from: "clawdbot/design_sources.py"
      to: "TWENTYFIRST_API_KEY env var"
      via: "graceful degradation check"
      pattern: "TWENTYFIRST_API_KEY"
---

<objective>
Create the 21st.dev REST API client and wire it into design_sources.py so curated component references are enriched with real component code snippets instead of text-only slug references.

Purpose: Replace text-only "21st.dev-style" pattern references with actual component code fetched from the 21st.dev REST API, providing LLM variant generators with concrete structural inspiration.
Output: `tools/twentyfirst_client.py` (new client), updated `clawdbot/design_sources.py` (async enrichment), and comprehensive tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-21st-dev-recraft-pipeline-integration/10-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From tools/recraft_client.py (pattern to mirror):
```python
RECRAFT_API_BASE = "https://external.api.recraft.ai/v1"
def _headers() -> dict[str, str]: ...
async def generate_image(prompt: str, *, style: str, width: int, height: int, model: str, timeout: float) -> dict[str, Any]: ...
async def generate_logo(business_name: str, industry: str, style: str) -> dict[str, Any]: ...
```

From clawdbot/design_sources.py (current exports):
```python
_CURATED_SOURCES = [
    {"source": "21st.dev", "title": "Split Hero CTA", "slug": "split-hero-cta",
     "industries": {"service", "dentist", ...}, "sections": ["hero", "proof", "cta"],
     "why": "..."},
    # ...
]
def resolve_design_sources(lead: dict[str, Any]) -> dict[str, Any]:
    # Returns {"sources": [...], "adaptation_rules": [...]}
```

From clawdbot/site_builder.py (consumers of design_sources):
```python
# Line 1185: design_source_payload = _resolve_design_sources(lead)
# Line 1218: json.dumps(design_source_payload.get("sources", []), indent=2)
# Line 1255: "design_sources": design_source_payload.get("sources", [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 21st.dev REST API client and tests</name>
  <files>tools/twentyfirst_client.py, tests/test_twentyfirst_client.py</files>
  <read_first>
    - tools/recraft_client.py (pattern to mirror for client structure)
    - tools/runtime_honesty.py (env_is_configured pattern)
    - .planning/phases/10-21st-dev-recraft-pipeline-integration/10-RESEARCH.md (API details)
  </read_first>
  <behavior>
    - Test 1: fetch_component_inspiration returns dict with "text" and "search_query" keys on success
    - Test 2: fetch_component_inspiration returns empty text when API key is missing (graceful degradation)
    - Test 3: fetch_component_inspiration returns empty text on HTTP error (non-fatal)
    - Test 4: fetch_component_inspiration returns empty text on timeout (non-fatal)
    - Test 5: fetch_component_inspiration truncates response text to max_chars (default 1500)
    - Test 6: get_twentyfirst_status returns blocked when TWENTYFIRST_API_KEY not set
    - Test 7: get_twentyfirst_status returns live when TWENTYFIRST_API_KEY is set
  </behavior>
  <action>
    Create tools/twentyfirst_client.py mirroring recraft_client.py pattern (per TWENTY1-01):

    1. Constants: TWENTYFIRST_API_BASE = "https://magic.21st.dev"
    2. get_twentyfirst_status() using env_is_configured("TWENTYFIRST_API_KEY") and truth_payload (same pattern as get_recraft_status)
    3. _headers() returning {"Content-Type": "application/json", "x-api-key": os.getenv("TWENTYFIRST_API_KEY", "")}
    4. async def fetch_component_inspiration(message: str, search_query: str, *, max_chars: int = 1500, timeout: float = 30.0) -> dict[str, Any]:
       - Check _check_twentyfirst_available() first; if unavailable, return {"text": "", "search_query": search_query, "reason": "unavailable"}
       - POST to f"{TWENTYFIRST_API_BASE}/api/fetch-ui" with json={"message": message, "searchQuery": search_query}
       - On success: return {"text": data.get("text", "")[:max_chars], "search_query": search_query}
       - On any exception: log warning, return {"text": "", "search_query": search_query, "error": str(e)}
    5. Logger: logging.getLogger("perseus.tools.twentyfirst")
    6. Import from tools.runtime_honesty: env_is_configured, truth_payload

    Create tests/test_twentyfirst_client.py:
    - Use monkeypatch to mock TWENTYFIRST_API_KEY env var
    - Use unittest.mock.AsyncMock to mock httpx.AsyncClient
    - Test each behavior above
    - Follow pattern from existing test files (importlib.import_module style)
  </action>
  <verify>
    <automated>PYTHONPATH=. python3 -m pytest tests/test_twentyfirst_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "fetch_component_inspiration" tools/twentyfirst_client.py returns >= 1
    - grep -c "TWENTYFIRST_API_BASE" tools/twentyfirst_client.py returns >= 1
    - grep -c "magic.21st.dev" tools/twentyfirst_client.py returns >= 1
    - grep -c "x-api-key" tools/twentyfirst_client.py returns >= 1
    - grep -c "max_chars" tools/twentyfirst_client.py returns >= 1
    - grep -c "def test_" tests/test_twentyfirst_client.py returns >= 5
    - PYTHONPATH=. python3 -m pytest tests/test_twentyfirst_client.py -x -q exits 0
  </acceptance_criteria>
  <done>21st.dev client exists, calls POST /api/fetch-ui, degrades gracefully when key missing or API fails, all 7+ tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire component enrichment into design_sources.py and extend tests</name>
  <files>clawdbot/design_sources.py, tests/test_design_sources.py</files>
  <read_first>
    - clawdbot/design_sources.py (current implementation)
    - tests/test_design_sources.py (existing test)
    - tools/twentyfirst_client.py (just created in Task 1)
    - clawdbot/site_builder.py lines 1179-1260 (_generate_reference_strategy — consumer)
  </read_first>
  <behavior>
    - Test 1: resolve_design_sources_with_components adds component_snippets to 21st.dev sources when API returns data
    - Test 2: resolve_design_sources_with_components returns same structure as resolve_design_sources when API unavailable
    - Test 3: component_snippets have "section" and "code" keys
    - Test 4: each component snippet code is truncated to max 1500 chars
    - Test 5: max 2 component fetches per curated source (not unlimited)
    - Test 6: adaptation_rules still present in enriched output
    - Test 7: existing test_resolve_design_sources_merges_catalog_and_research_references still passes
  </behavior>
  <action>
    Modify clawdbot/design_sources.py (per TWENTY1-02, TWENTY1-03):

    1. Add new async function resolve_design_sources_with_components(lead: dict[str, Any]) -> dict[str, Any]:
       - Call existing resolve_design_sources(lead) to get base payload
       - Extract industry from lead
       - For each source in payload["sources"] where source["source"] == "21st.dev":
         - For each section in source.get("sections", [])[:2] (max 2 fetches per source):
           - Call fetch_component_inspiration(message=f"{section} section for {industry} business website", search_query=f"{section} {source.get('title', '')}")
           - If snippet["text"] is non-empty, append to source.setdefault("component_snippets", []) as {"section": section, "code": snippet["text"][:1500]}
       - Add adaptation rule (per TWENTY1-03): "Component code from 21st.dev is React/TSX — use as structural inspiration for HTML+Tailwind, never copy JSX syntax directly."
       - Return enriched payload
       - Wrap entire enrichment in try/except: on any error, log warning and return base payload unchanged (graceful degradation)

    2. Import at top of file: from tools.twentyfirst_client import fetch_component_inspiration
       - Use conditional import inside the async function to avoid import errors when client unavailable

    3. Keep resolve_design_sources unchanged (backward compatible)

    Extend tests/test_design_sources.py:
    - Add async tests using pytest-asyncio (or use synchronous mocking)
    - Mock fetch_component_inspiration to return controlled snippets
    - Test enrichment adds component_snippets to 21st.dev sources
    - Test graceful degradation when fetch returns empty
    - Ensure existing test still passes unchanged
  </action>
  <verify>
    <automated>PYTHONPATH=. python3 -m pytest tests/test_design_sources.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "resolve_design_sources_with_components" clawdbot/design_sources.py returns >= 1
    - grep -c "component_snippets" clawdbot/design_sources.py returns >= 1
    - grep -c "fetch_component_inspiration" clawdbot/design_sources.py returns >= 1
    - grep -c "structural inspiration" clawdbot/design_sources.py returns >= 1
    - grep -c "def test_" tests/test_design_sources.py returns >= 4
    - PYTHONPATH=. python3 -m pytest tests/test_design_sources.py -x -q exits 0
  </acceptance_criteria>
  <done>design_sources.py has async resolve_design_sources_with_components that enriches 21st.dev sources with real component code, degrades gracefully, existing tests unbroken, 7+ new tests pass</done>
</task>

</tasks>

<verification>
- PYTHONPATH=. python3 -m pytest tests/test_twentyfirst_client.py tests/test_design_sources.py -x -q
- grep -r "fetch_component_inspiration" tools/ clawdbot/ -- confirms wiring
- grep "component_snippets" clawdbot/design_sources.py -- confirms enrichment path
</verification>

<success_criteria>
- 21st.dev REST client exists at tools/twentyfirst_client.py and is callable
- design_sources.py has async enrichment function that fetches real component code
- All tests pass (12+ new tests across both files)
- Missing TWENTYFIRST_API_KEY degrades to current text-only behavior
- Component snippets truncated to 1500 chars, max 2 per source
</success_criteria>

<output>
After completion, create `.planning/phases/10-21st-dev-recraft-pipeline-integration/10-01-SUMMARY.md`
</output>
